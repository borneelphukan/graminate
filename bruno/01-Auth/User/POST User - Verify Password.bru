meta {
  name: "User - Verify Password"
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/user/verify-password/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "password": "password123"
  }
}
