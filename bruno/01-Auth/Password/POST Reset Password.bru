meta {
  name: "Reset Password"
  type: http
  seq: 32
}

post {
  url: {{baseUrl}}/api/password/reset
  body: json
  auth: none
}

body:json {
  {
    "email": "user@example.com",
    "token": "string",
    "newPassword": "newpassword123"
  }
}
