meta {
  name: "Create Order"
  type: http
  seq: 35
}

post {
  url: {{baseUrl}}/api/payment/create-order
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "amount": 100,
    "currency": "INR"
  }
}
